name: Update Tech Stack
on:
  workflow_dispatch:   # 手动触发
  push:
    branches: [main]   # push 到 main 时自动运行
  schedule:
    - cron: '0 0 * * 0'  # 每周日 00:00 UTC 自动运行

permissions:
  contents: write

env:
  PROFILE_USERNAME: James-Zeyu-Li

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install deps
        run: pip install requests

      - name: Generate tech tables
        env:
          # Read-only fine-grained PAT for Filmory-Web only. It is used solely
          # for GitHub API requests; actions/checkout retains GITHUB_TOKEN for
          # committing README.md.
          FILMORY_ACCESS_KEY: ${{ secrets.FILMORY_ACCESS_KEY }}
          # Used for public GitHub API requests and committing README.md.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PROFILE_USERNAME: ${{ env.PROFILE_USERNAME }}
        run: python scripts/gen_techstack.py

      - name: Commit changes
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add README.md
            git commit -m "chore: refresh tech tables"
            git push
          fi
